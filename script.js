//your JS code here. If required.
function functionName() {
  // Get the name of the current function using arguments.callee.name
  const currentFunctionName = arguments.callee.name;

  // Display the name using an alert statement
  alert( currentFunctionName);
}